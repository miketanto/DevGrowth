You are resuming work on feature: $ARGUMENTS

This is a fresh context window. Read these files to reconstruct your state:

1. Find your worklog: Glob for `*$ARGUMENTS*` in `/Users/michaelsutanto/Documents/Mike's Vault/7 - Project Logs/DevGrowth/Worklogs/`. Find the Resume context block.
2. Find your feature doc: Glob for `*$ARGUMENTS*` in `/Users/michaelsutanto/Documents/Mike's Vault/7 - Project Logs/DevGrowth/Features/`. Read scope + acceptance criteria.
3. `/Users/michaelsutanto/Documents/Mike's Vault/7 - Project Logs/DevGrowth/Claude/SWE Playbook.md`
4. `CLAUDE.md` in repo root

Then: `git status`, `git log --oneline -5`, `git diff --stat` to confirm state matches worklog.

Announce what was done (from worklog) and what's next (from "What's left").
Continue from where the previous session left off.
